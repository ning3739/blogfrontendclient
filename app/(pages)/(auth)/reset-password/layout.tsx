export const metadata = {
  title: "重置密码 | Reset-Password",
  description: "小李生活志重置密码页面",
  keywords: "重置密码, Reset password",
};

export default function ForgotPasswordLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}
